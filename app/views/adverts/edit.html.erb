<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/home/index">Home</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to 'Novo anúncio', new_advert_path %></li>
        <% else %>
          <li>
            <%= link_to('Login', new_user_session_path) %>
          </li>

        <% end %>
      </ul>
      <form class="navbar-form navbar-left">
      </form>
      <% if user_signed_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email%><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Editar conta", edit_user_registration_path %></li>
              <%user = current_user %>
              <li><%= link_to 'Meus anúncios', adverts_path(:id=>user.id)%></li>
              <%if current_user.admin? == true %>
                <li><%= link_to "Usuários", users_path %></li>
                <li><%= link_to "Criar categoria", new_category_path %></li>
                <li><%= link_to "Categorias", categories_path %></li>
              <%end %>
              <li role="separator" class="divider"></li>
              <li><a <%= link_to('Sair', destroy_user_session_path, method: :delete) %></a></li>

            </ul>
      <% end %>
      </li>
      </ul>
    </div><!-- /.navbar-collapse -->

  </div><!-- /.container-fluid -->
</nav>

<div class="container-fluid col-xs-6 col-md-4 col-md-offset-4">

  <br>
  <br>
  <%advert = @advert %>
  <%if can? :manage, advert %>
    <div class="panel-heading">
      <h3 class="panel-title"><strong><h3 class="text-center">Editar informações do anúncio</h3></strong></h3>
    </div>

    <%= render 'form', advert: @advert %>

    <%= link_to 'Visualizar', @advert, :class => "btn btn-info" %> |
    <%= link_to 'Voltar', adverts_path, :class => "btn btn-primary"%>
  <%else %>
    <h1>Você não pode editar um anúncio que não lhe pertence</h1>
  <%end %>

</div>
